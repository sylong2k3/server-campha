'use strict';

// Compatibility facade. New code should import `services/satellite` so the
// request, Earth Engine builders, cache and publish concerns remain isolated.
module.exports = require('./satellite');
