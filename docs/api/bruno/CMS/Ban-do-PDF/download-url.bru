meta {
  name: URL tai ban do PDF
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/cms/pdf-maps/{{pdfMapId}}/download-url?expireSeconds=300
  body: none
  auth: bearer
}

auth:bearer {
  token: {{citizenToken}}
}

tests {
  test("200 OK", function() { expect(res.getStatus()).to.equal(200); });
}
