meta {
  name: Dat lai mat khau
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/auth/reset-password
  body: json
  auth: none
}

body:json {
  {
    "token": "<reset-token-tu-link-email>",
    "newPassword": "NewPass@2026"
  }
}

tests {
  test("200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
