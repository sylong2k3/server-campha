meta {
  name: Xac minh Email
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/auth/verify-email
  body: json
  auth: none
}

body:json {
  {
    "token": "<token-tu-link-trong-email>"
  }
}

tests {
  test("200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
