meta {
  name: Login - system_admin
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "admin@campha.gov.vn",
    "password": "CamPha@2026"
  }
}

script:post-response {
  const d = res.getBody().data;
  if (d && d.accessToken) {
    bru.setEnvVar("adminToken", d.accessToken);
    bru.setEnvVar("refreshToken", d.refreshToken);
  }
}

tests {
  test("200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
