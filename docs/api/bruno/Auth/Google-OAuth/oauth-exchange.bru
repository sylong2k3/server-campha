meta {
  name: Google OAuth - Doi code lay token
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/auth/oauth/exchange
  body: json
  auth: none
}

body:json {
  {
    "code": "<short-lived-code-tu-callback>"
  }
}

tests {
  test("200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
