meta {
  name: Chi tiet Feature
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/api/v1/web-map/layers/{{layerId}}/features/{{featureId}}?includeGeometry=false
  body: none
  auth: bearer
}

auth:bearer {
  token: {{citizenToken}}
}

tests {
  test("200 OK", function() { expect(res.getStatus()).to.equal(200); });
}
