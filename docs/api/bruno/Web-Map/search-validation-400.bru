meta {
  name: Tim kiem - validation loi (400)
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/web-map/features/search?q=x&limit=100
  body: none
  auth: none
}

tests {
  test("400 Bad Request", function() { expect(res.getStatus()).to.equal(400); });
}
