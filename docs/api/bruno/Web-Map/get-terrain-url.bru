meta {
  name: URL tile Terrain
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/api/v1/web-map/terrain/{{layerId}}/url?expireSeconds=300
  body: none
  auth: bearer
}

auth:bearer {
  token: {{citizenToken}}
}

tests {
  test("200 OK", function() { expect(res.getStatus()).to.equal(200); });
}
