meta {
  name: Danh sach tram quan trac
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/admin/kttv/stations?page=1&limit=20
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("200 OK", function() { expect(res.getStatus()).to.equal(200); });
}
